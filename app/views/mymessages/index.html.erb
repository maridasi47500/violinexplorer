<p id="notice"><%= notice %></p>

<h1>Mes messages</h1>
<p>Mes messages pour la radio/télé comme "mistral fm", "tous les hits" et "maintenant sur le 92.4"</p>
<table>
  <thead>
    <tr>
      <th>Content</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @mymessages.each do |mymessage| %>
      <tr>
        <td><%= mymessage.content %></td>
        <td><%= mymessage.user_id %></td>
        <td><%= link_to 'Show', mymessage %></td>
        <td><%= link_to 'Edit', edit_mymessage_path(mymessage) %></td>
        <td><%= link_to 'Destroy', mymessage, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Mymessage', new_mymessage_path %>
