<%= form_with(model: ytlink, local: true) do |form| %>
  <% if ytlink.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ytlink.errors.count, "error") %> prohibited this ytlink from being saved:</h2>

      <ul>
        <% ytlink.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link %>
  </div>

  <div class="field">
    <%= form.label :song_id %>
    <%= form.number_field :song_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
